#CheatSheet!

"gcloud config configurations list"
Lists all available configurations

"gcloud config configurations activate PROJECT_NAME"
can be used to change project

"gcloud compute instances list"
Lists all VM instances

"gcloud compute instance start NAME"

"gcloud compute config-ssh"
Makes sshing into VMs easier memorable alias to ~/.ssh/config

"gcloud compute firewall-rules create NAME --allow tcp:8000-9000,icmp"
Allows access to all ports in the range [8000,9000]

"gcloud compute instance-groups managed resize NAME --size=SIZE" will change the amount of machines in a group to SIZE.
